/* 
You are on an expedition in search of a lost treasure. To locate the treasure, you have a map with clues. Return the position of the element that represents the treasure.
*/

const map = ["Forest", "Montain", "Cavern", "Treasure", "River"]
const treasure = "Treasure"
const res = map.indexOf(treasure)
console.log(res)