/* 
Declare two variables, f and g, both initialized as undefined. Print to the console if these variables are strictly equal.
*/

let f, g

if (f === g) {
   console.log('Strictly equal')
} else {
   console.log('Not equal')
}
