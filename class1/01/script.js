/* 
Declare two variables, a and b, assigning them different boolean values. Then, create a condition that checks if both are true.
 */

const a = true
const b = false

if (a === true && b === true) {
    console.log('True')
} else {
    console.log('False')
}