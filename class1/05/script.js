/* 
Declare an empty object called c. Add properties to this object, such as name and age, and print the object to the console.
*/

const c = {
   name: "Brienne",
   color: "Orange",
   gender: "Female",
}
const res = `${c.name} is a ${c.gender} ${c.color} cat.`
console.log(res)