/* 
Create a variable called "combination" and assign to it a value that is a combination of at least two different types (for example, a number and a string).
*/

const combination = "The combination of " + 1 + " + " + 2 + " is " + 3 + "."

console.log(combination)